name: FTP Deploy to cPanel

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.0

        with:
          server: ftp.institutodatatrends.com
          username: instit14
          password: "I1tCt)9Gy[58wT"
          protocol: ftp
          port: 21
          passive: true
          security: loose
          timeout: 60000


          