on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: 🔍 Test FTP Connection
      run: |
        echo "Testando conexão FTP..."
        apt-get update && apt-get install -y ftp
        ftp -n <<EOF
        open ftp.institutodatatrends.com
        user instit14 ${{ secrets.FTP_PASSWORD }}
        bye
        EOF
        echo "Teste FTP completo"
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.6
      with:
        server: institutodatatrends.com
        username: instit14
        password: I1tCt)9Gy[58wT
        server-dir: public_html/

